name = "ads-redirect-service"
compatibility_date = "2024-10-01"
compatibility_flags = ["nodejs_compat"]
main = "redirect-worker.js"

# 环境变量
[vars]
# 可以添加一些配置变量

# 允许所有域名访问（用于重定向服务）
[global_handlers]
handlers = ["redirect"]