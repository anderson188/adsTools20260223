name = "ads-automation-api"
compatibility_date = "2024-10-01"
compatibility_flags = ["nodejs_compat"]
main = "index.js"

# D1数据库配置
database_id = "a39c88b7-2ccc-490b-8d23-d55fd0eb93c3"
database_name = "ads_automation"

# 环境变量 (在实际部署时需要设置)
[vars]
JWT_SECRET = "your-jwt-secret-key"
GOOGLE_ADS_DEVELOPER_TOKEN = "your-developer-token"
GOOGLE_ADS_CLIENT_ID = "your-client-id"
GOOGLE_ADS_CLIENT_SECRET = "your-client-secret"
GOOGLE_ADS_REFRESH_TOKEN = "your-refresh-token"
# WORKER_DOMAIN = "your-worker.workers.dev"

# 定时任务配置
[triggers]
cron = ["*/5 * * * *"]  # 每5分钟执行一次定时任务

# 路由配置 - 暂时注释掉，使用默认的 workers.dev 域名进行测试
# [[routes]]
# pattern = "api.ads-tools.example.com/*"
# zone_name = "ads-tools.example.com"